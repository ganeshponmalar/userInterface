# Vite React App

Minimal Vite + React starter created by assistant.

To run:

```bash
npm install
npm run dev
```
# Login UI (Vite + React)

Minimal demo app with a responsive login form, raw CSS, and basic tests.

Install and run:

```bash
cd /home/ganesan/Downloads/UserInterface
npm install
npm run dev
```

Run tests:

```bash
npm run test
```
